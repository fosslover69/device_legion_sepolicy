# Allow Settings to read and set theme_prop
get_prop(priv_app, theme_prop)
set_prop(priv_app, theme_prop)

# Allow Settings to read spectrum_prop
get_prop(priv_app, spectrum_prop)
set_prop(priv_app, spectrum_prop)

